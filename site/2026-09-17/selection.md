# 首次试运行的选题审计

本次续做先前已选中的论文，没有为了更换结果而重新抽取。
本会话先前执行记录显示：2026-09-17T02:36:19Z，用 `secrets.randbelow(3)` 得到 1，选择金融方向。
2026-09-17T02:45:13Z，用 `secrets.randbelow(364)` 得到零基索引 137，抽到 `2608.23416v2`。
这两个时间和数值恢复自先前会话记录；本次没有重放或伪造那次随机调用。
当前直接检查保留下来的 `/tmp/fin_candidates.json`，确认条数为 364，索引 137 的标题及 ID 与本篇一致。

## 候选池与已完成核对

原始检索收集 q-fin 子类的近期提交，按基础 ID 去重，筛选首次提交日期不早于 2026-06-19；检索时没有已有 history.jsonl。
q-fin.EC 的检索当时为空，其余结果组成这份有限检索池。
保存的候选池含标题、摘要、类别、ID、版本和提交日期，保存在本地学习包 `.source/` 中，不把其他论文的整批摘要发布到站点。
本次已经核对所选论文的官方 arXiv 信息页和 HTML 正文：Jiayu Li，首次提交 2026-08-24，v2 修订 2026-09-02；正文及核心公式、实验表格可以读取。
本篇按金融研究问题归类，arXiv 本身的标签为 cs.LG / q-fin.PM。

## 必须保留的流程缺口

旧脚本只完成了类别、日期和去重过滤，未证明 364 篇都在抽取前逐篇通过标题/摘要适配性与正文可读性审核。
因此这份记录只能称为“从检索池随机抽取后，对本篇补做资格核验”，不能改称“从事前全部合格的池中抽取”。
这与原任务要求的筛选先后顺序存在差异，首次试运行的选题流程不能据此宣称完全验收通过。
保留该差异并续做已经选定的论文，可以避免隐瞒或为了改变结果重复抽签。
后续每日运行应先保存完成资格审核的候选池，再在该池内执行并持久化随机抽取记录。
任何一次都不代表对 arXiv 全库的均匀抽样。

## 学习状态

材料制作、数学算例通过测试与学习者掌握是不同状态。
当前没有学习者对这篇论文的实际作答，所有练习均保持待验证。
